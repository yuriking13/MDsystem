# 🤖 AI-поиск статей в графе цитирований

## Текущая реализация

AI Assistant уже реализован! Endpoint: `POST /api/projects/:id/graph-ai-assistant`

### Как работает

1. **Контекст графа**: Передаются все загруженные статьи (level 0, 2, 3)
2. **OpenRouter API**: Используется GPT-4 для анализа
3. **Умный поиск**: AI находит релевантные статьи среди уже загруженных
4. **Автоподсветка**: Найденные статьи подсвечиваются на графе

### Запросы которые работают

```
"Найди статьи о machine learning в медицине"
"Покажи систематические обзоры"
"Найди мета-анализы последних 5 лет"
"Статьи с высоким импакт-фактором"
"Работы из Nature или Science"
"Статьи цитирующие Smith 2020"
```

### UI

Кнопка **"AI ✨"** открывает панель справа от графа:

- Текстовое поле для запроса
- История диалога
- Список найденных статей
- Кнопки быстрого добавления в проект

### Примеры использования

#### 1. Поиск по методологии

```
Пользователь: "Найди рандомизированные контролируемые испытания"
AI: Нашёл 12 статей (RCT)
→ Подсвечиваются на графе голубым
→ Кнопка "Добавить все в проект"
```

#### 2. Поиск по журналу

```
Пользователь: "Покажи статьи из Lancet"
AI: 7 публикаций из The Lancet (2018-2023)
→ Список с PMID и годами
→ Клик на статью → детали
```

#### 3. Поиск по цитированиям

```
Пользователь: "Самые цитируемые работы про диабет"
AI: Топ-10 по цитированиям
→ Сортировка по убыванию
→ Показ количества цитирований
```

---

## 🚀 Улучшения для Фазы 2

### 1. Расширенный промпт

**Текущий**:

```
Найди статьи по запросу "{query}" среди {count} статей графа
```

**Улучшенный**:

```
Ты - эксперт по научной литературе. Анализируешь граф цитирований проекта "{projectName}".

Контекст проекта:
- Тип исследования: {researchType}
- Годы: {yearRange}
- Статей в проекте: {articleCount}
- Ключевые темы: {extractedTopics}

Запрос пользователя: "{query}"

Среди {externalCount} загруженных внешних статей найди наиболее релевантные.

Критерии отбора:
1. Соответствие тематике проекта
2. Методологическая значимость
3. Количество цитирований
4. Свежесть публикации (если не указано иное)
5. Репутация журнала

Верни JSON:
{
  "foundPmids": ["12345", "67890", ...],
  "reasoning": "Краткое объяснение выбора",
  "suggestions": [
    {
      "pmid": "12345",
      "relevanceScore": 0.95,
      "reason": "Ключевая методологическая работа",
      "priority": "high"
    }
  ]
}

Макс 20 статей, отсортированных по релевантности.
```

### 2. Предзагрузка контекста

```typescript
// Экстракция тем из абстрактов проекта
const projectTopics = await extractTopics(projectArticles);

// Передача в AI
const aiContext = {
  projectName: project.name,
  researchType: project.research_type,
  topics: projectTopics, // ["diabetes", "insulin", "glycemic control"]
  yearRange: { min: 2015, max: 2023 },
  articleCount: 42,
};
```

### 3. Semantic search (векторный поиск)

**План**:

1. Генерируем embeddings для всех статей графа
2. Embedding запроса пользователя
3. Cosine similarity для ранжирования
4. Топ-K статей передаём в GPT для финального отбора

**Преимущества**:

- Быстрее (векторный поиск вместо полного LLM)
- Точнее (учитывает семантическую близость)
- Дешевле (меньше токенов в GPT)

```typescript
// Псевдокод
async function semanticSearchArticles(query: string, articles: Article[]) {
  const queryEmbedding = await getEmbedding(query);

  const scored = articles.map((a) => ({
    article: a,
    score: cosineSimilarity(queryEmbedding, a.embedding),
  }));

  const topK = scored.sort((a, b) => b.score - a.score).slice(0, 50); // Топ-50 для дальнейшего анализа GPT

  // GPT финализирует выбор с контекстом проекта
  return await gptFinalSelection(topK, projectContext);
}
```

### 4. Multi-turn диалог

**Текущее**: Каждый запрос независим  
**Улучшение**: Контекст предыдущих ответов

```typescript
// Сохранение истории
const aiHistory = [
  { role: "user", content: "Найди статьи про диабет" },
  { role: "assistant", content: "Нашёл 15 статей..." },
  { role: "user", content: "Покажи только мета-анализы" }, // ← Контекст!
];

// Передача в GPT
const response = await openrouter.chat({
  messages: [{ role: "system", content: systemPrompt }, ...aiHistory],
});
```

---

## 🎯 Метрики эффективности

### Текущие показатели

- **Точность**: ~70-80% (зависит от промпта)
- **Скорость**: 3-5 сек на запрос
- **Стоимость**: ~$0.01-0.03 за запрос (GPT-4)

### Целевые показатели (после улучшений)

- **Точность**: >90% (semantic search + GPT)
- **Скорость**: 1-2 сек (векторный поиск + кэш)
- **Стоимость**: <$0.005 за запрос (меньше токенов)

---

## 💡 Примеры продвинутых запросов

### Временные фильтры

```
"Статьи последних 2 лет о CRISPR"
"Работы 2015-2018 годов"
"Самые свежие публикации"
```

### Комбинированные фильтры

```
"RCT из топ-10 журналов с >100 цитированиями"
"Систематические обзоры на русском языке"
"Статьи авторов из США про иммунотерапию"
```

### Связи в графе

```
"Статьи цитирующие Smith 2020 и Jones 2019 одновременно"
"Работы которые никто не цитирует (orphans)"
"Мосты между кластерами"
```

### Качественный анализ

```
"Статьи с высоким методологическим качеством"
"Работы с открытым доступом к данным"
"Публикации с зарегистрированным протоколом"
```

---

## 🔧 Технические требования

### База данных

```sql
-- Таблица для embeddings (будущее)
CREATE TABLE article_embeddings (
  article_id UUID PRIMARY KEY REFERENCES articles(id),
  embedding vector(1536), -- OpenAI text-embedding-3-small
  model VARCHAR(50) DEFAULT 'text-embedding-3-small',
  generated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_embeddings_cosine ON article_embeddings
  USING ivfflat (embedding vector_cosine_ops);
```

### Зависимости

```json
{
  "dependencies": {
    "openai": "^4.x", // Для embeddings API
    "pgvector": "^0.2.x" // PostgreSQL extension
  }
}
```

### Environment

```env
OPENAI_API_KEY=sk-... # Для embeddings (дешевле чем GPT-4)
```

---

## 📈 Roadmap

### ✅ Текущее состояние (Фаза 1)

- Базовый AI поиск работает
- Передача контекста графа
- Подсветка найденных статей
- Быстрое добавление в проект

### 🔄 В разработке (Фаза 2)

- Улучшенные промпты
- Semantic search с embeddings
- Multi-turn диалог
- Кэширование результатов

### 🔮 Будущее (Фаза 3)

- Link prediction (скрытые связи)
- Автоматические кластеры
- Временная динамика
- Рекомендации на основе ML

---

## 🎓 Best practices

### 1. Формулируйте точные запросы

❌ "Найди что-нибудь интересное"  
✅ "Найди мета-анализы про эффективность метформина при диабете 2 типа"

### 2. Используйте фильтры

❌ "Статьи про COVID"  
✅ "Статьи 2020-2022 про COVID вакцины из рецензируемых журналов"

### 3. Уточняйте в диалоге

```
1. "Найди статьи про антибиотики"
2. "Покажи только про резистентность"
3. "Из них выбери мета-анализы"
```

### 4. Проверяйте результаты

- Читайте reasoning AI
- Проверяйте relevanceScore
- Открывайте абстракты для валидации
